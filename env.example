# File di esempio per variabili d'ambiente
# Copia questo file come .env e inserisci i tuoi valori reali
# Comando: cp env.example .env
#
# NOTA: I valori di default sono presi dalla configurazione esistente del progetto
# Per produzione, modifica questi valori con le tue credenziali reali
#
# ============================================================================
# DEPLOYMENT SU RENDER.COM
# ============================================================================
# Per deploy su Render, configura queste variabili nel Dashboard Render:
# Dashboard → Il tuo Web Service → Environment → Add Environment Variable
#
# IMPORTANTE per Render:
# - CANVA_REDIRECT_URI deve usare l'URL del tuo service Render:
#   https://tuo-app.onrender.com/api/canva/callback
# - PORT viene assegnato automaticamente da Render (non serve configurarlo)
# - Usa Persistent Disks per salvare database e file (vedi DEPLOY_RENDER.md)
# ============================================================================

# OpenAI API Key (obbligatorio per funzionalità AI)
# Valore preso dal file .env esistente del progetto
OPENAI_API_KEY=your-openai-api-key-here

# Canva OAuth Credentials
# IMPORTANTE: Sostituisci con le tue credenziali Canva reali
CANVA_CLIENT_ID=your-canva-client-id-here
CANVA_CLIENT_SECRET=your-canva-client-secret-here
# Canva Redirect URI
# Per sviluppo locale:
CANVA_REDIRECT_URI=http://localhost:5001/api/canva/callback
# Per Render (sostituisci "tuo-app" con il nome del tuo service):
# CANVA_REDIRECT_URI=https://tuo-app.onrender.com/api/canva/callback

# Secret Key per sessioni Flask
# Per produzione, genera una chiave forte con: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-in-production

